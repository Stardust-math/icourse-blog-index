name: Update dataset

on:
  schedule:
    - cron: "37 19 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: icourse-crawler
  cancel-in-progress: false

jobs:
  maintenance:
    name: Daily maintenance
    uses: ./.github/workflows/_maintenance.yml
    permissions:
      contents: write
