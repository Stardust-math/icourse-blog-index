# 数据说明与负责任使用

本仓库是非官方项目，目的是为评课社区公开用户资料中由用户自行填写的博客链接建立一个可审计、可更新的索引。它不代表评课社区或中国科学技术大学，也不表示站方已经对本项目作出正式授权或认可。

## 收集范围

规范数据位于 `data/users/*.jsonl`。每个已经尝试访问的数字 ID 都会留下记录，包括公开主页、隐藏主页、明确不存在的 ID 和暂时无法确认的 ID。这样可以复核一次更新为何改变或没有改变某条博客记录，也可以在以后重新检查曾经缺失或隐藏的 ID。

本仓库只保存完成索引和审计所需的信息：

- 用户数字 ID及公开主页地址；
- 主页是否公开、隐藏、明确不存在或尚未确认；
- 公开资料中的博客原始值与规范化 URL（如有）；
- 首次检查、最近尝试、最近成功确认和爬虫观察到变化的时间；
- 请求或解析结果、连续失败次数、解析器版本和规范字段指纹；
- 尚待第二次确认的博客变化候选值；
- 博客 URL 的有限可访问性检查结果。

本项目不会收集或推断用户名、昵称、头像、个人简介、点评、关注关系、邮箱、学号或其他与博客索引无关的个人信息；不会保存 Cookie、原始 HTML、完整响应头或博客正文；不会登录、调用隐藏接口、绕过访问控制或从其他来源补全私人信息。

“完整用户记录”指所有已尝试 ID 的必要索引状态，不表示本仓库包含站内所有个人资料，也不保证每个数字 ID 对应一名真实或仍然存在的用户。

## 状态语义

`profile_status` 与 `blog_status` 必须组合解释：

| `profile_status` | `blog_status` | 含义 |
|---|---|---|
| `public` | `present` | 成功解析公开主页并确认存在博客 |
| `public` | `absent` | 成功解析公开主页并确认没有博客 |
| `hidden` | `unknown` | 主页明确隐藏，无法判断博客 |
| `missing` | `unknown` | 页面正文明确表示用户不存在（包括 HTTP 200 的软 404） |
| `unknown` | `unknown` | 网络、限流、拦截或解析问题使本次结果不确定 |

临时错误不会抹去上一次已经确认的主页或博客状态。博客的新增、删除和修改只有在独立复查得到一致结果后才会进入正式字段；冲突结果保存在 `pending_observation` 中等待后续确认。

`last_checked_at` 是最近一次尝试时间，`last_confirmed_at` 是最近一次成功解析并确认状态的时间。`profile_changed_at` 和 `blog_changed_at` 只是爬虫首次观察到已确认变化的时间，并非用户在评课社区实际编辑资料的时间。

## 来源、缓存与准确性

资料状态仅以 `https://icourse.club/user/{id}` 的直接响应为准，不使用搜索引擎摘要、网页存档或 AI 缓存。请求要求重新验证缓存，解析器只读取资料区中明确标注为“博客”的字段。缓存状态异常、Cloudflare 验证页面、未知 HTML 和网络故障都不会被写成“用户不存在”或“没有博客”。

链接健康检查只能说明某次自动请求的结果。`403`、`429`、机器人拦截、超时或 DNS/TLS 故障不等于博客已经失效；只要用户主页仍声明该链接，它就保留在索引中。仓库不会执行博客中的 JavaScript，也不会保存博客内容。

自动数据可能因网站结构变化、网络故障、缓存、用户修改资料或定时任务延迟而不完整或过时。请在作出重要判断前打开对应用户主页核对。

## 对源站的保护

爬虫保持单线程，默认在用户页请求之间随机等待 2.5–3.5 秒。初始化与日常更新共享同一个 GitHub Actions 并发锁，不能同时运行。遇到标准 `robots.txt` 禁止规则、持续的 `403`/`429`、Cloudflare 挑战、显著解析异常或异常变化率时，爬虫会退避或停止；它不会更换代理、轮换 IP 或尝试绕过限制。

截至 2026-08-30，站点的 [`robots.txt`](https://icourse.club/robots.txt) 实时响应包含 Cloudflare 的 content-signal 说明性注释，但没有给出 `User-agent`、`Allow`、`Disallow`、`Crawl-delay` 指令或具体的 content-signal 取值；标准 robots 解析因此未发现针对 `/user/` 的禁令。没有明确禁止不等于正式许可，自动化每次运行仍会重新检查实际目标 URL，并以新的站点规则为准。访问公开用户页还可能增加站内显示的访问计数（参见站点的[公开路由源码](https://github.com/USTC-iCourse/ustc-course/blob/7980aad28148caba013c81020e338ed06e7993a7/app/views/user.py#L12-L31)），因此初始化被刻意限速且不使用并发。

## 更正与移除

如发现链接错误、解析错误，或希望讨论与本人记录有关的更正或移除，请在本仓库启用 Issues 后提交 Issue，附上相关数字 ID 和可以公开核验的信息；如 Issues 未启用，请通过仓库所有者 GitHub 主页提供的联系方式联系维护者。不要在公开 Issue 中提交邮箱、学号、Cookie 或其他私人信息。

## 许可边界

根目录 `LICENSE` 的 MIT 许可证适用于本仓库的软件代码。它不声称授予源网站、用户资料、博客内容、商标或第三方链接内容的权利。公开事实型索引数据的使用仍应遵守适用法律、源网站规则和当事人的合理隐私请求。
